{
  "name": "@oldfolio/vault",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. packages/vault/src"
  },
  "dependencies": {
    "@oldfolio/domain": "workspace:*",
    "@oldfolio/okf": "workspace:*"
  }
}
